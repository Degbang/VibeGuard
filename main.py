"""VibeGuard CLI entry point.

Orchestrates the five-layer pipeline against a directory of Java
sample apps and produces the final risk report. Implemented once the
layers it depends on exist (see IMPLEMENTATION_LOG.md build order).
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("CLI entry point pending Layer 1-5 implementation")


if __name__ == "__main__":
    main()
